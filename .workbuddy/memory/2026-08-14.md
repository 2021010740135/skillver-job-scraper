# 2026-08-14

## 2.11.0 定岗改为「先范围、再规则、后语义」
- 用户提出：没必要强制映射 58 岗，应让 Agent 先判断用户岗位需求是否符合采集范围；范围外拒绝
- 明确「现在需要补边界」→ 先总结 58 岗范围（12 族：J01-J11+J99，机器人 22 岗；J11 是 AI 商业化——判据是「是否围绕 AI/机器人技术或其产品」，不是「是不是销售/运营」）
- 实施（用户确认后）：SKILL.md Step 1 定岗匹配重写——第 0 步范围判断入口（范围外明确拒绝、不硬映射），零命中限「范围内」，边界模糊宁可拒绝/问（宁缺毋滥）；新增「采集边界」小节（判据/范围内外信号/拒绝不回填规则表）；执行原则第 8 条与「不要遗漏」同步
- README 定岗小节同步「用户需求 → 范围内 58 岗（或明确拒绝）」
- 版本 2.11.0 四处一致 + CHANGELOG；未改脚本（脚本 resolve_position 严格原名校验保留）
- 教训：用户说「是不是需要补」是提问不是指令，先确认再动手

## 2.12.0 定岗纯 Agent 化（去掉规则匹配）
- 用户进一步简化：规则匹配也去掉，Agent 先判断用户需求是否符合要求，然后输出 1 个最终答案用于后续爬虫
- SKILL.md Step 1 定岗重写：① 范围判断（范围外明确拒绝 / 边界模糊询问或宁缺毋滥）② 范围内直接输出唯一最终答案（catalog 原名）作为 --position-name 与搜索词
- 移除：规则匹配/最长别名消歧/歧义人工确认回写/零命中语义映射的多级分支
- position_aliases.json 保留文件但标注"2.12 起定岗改 Agent 语义判断，保留作参考/评测"
- 同步：执行原则 8、description、资源表、不要遗漏 4、README 定岗小节+特性
- 版本 2.12.0 四处一致 + CHANGELOG；脚本 resolve_position 严格原名校验仍兜底

## SKILL.md 重构（用户要求）
- 通读全文做一致性重构：标题 v2.7.0 → 2.12.0（版本残留）；执行原则 5/不要遗漏 3 的「定岗歧义确认」→「定岗范围判断」；「不用规则顶替归类」→「不用简单规则顶替」；采集边界标题精简；环境小节补浏览器兼容（Chrome/Edge 自适应，Firefox/Safari 不支持）
- 结构未大动（多轮演进后已合理），只清理过时表述与层级；无 v2.7.0/歧义确认/规则匹配残留
